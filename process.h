#ifndef PROCESS_H
#define PROCESS_H

#include "command.h"

int run1process(struct Command const * const, const int, int const, int const);
int runnprocess(struct Command const * const);

#endif // PROCESS_H
